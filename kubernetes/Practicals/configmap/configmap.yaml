apiVersion: v1
kind: ConfigMap
metadata:
  name: my-config
data:
  app.properties: |
    APP_ENV=production
    APP_DEBUG=false
    APP_VERSION=1.0.0
  database_url: "mysql://user:password@db:3306/mydb"
  log_level: "INFO"
