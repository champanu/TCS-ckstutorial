apiVersion: v1
kind: Pod
metadata:
  name: pod-config-volume
spec:
  containers:
  - name: app
    image: busybox
    command: ["sh", "-c", "sleep 3600"]
    volumeMounts:
    - name: config-volume
      mountPath: /etc/http/
  volumes:
  - name: config-volume
    configMap:
      name: httpd.conf
