apiVersion: v1
kind: Pod
metadata:
  name: pod-with-config
spec:
  containers:
  - name: app
    image: busybox
    command: ["sh", "-c", "echo $APP_ENV && echo $log_level && sleep 3600"]
    envFrom:
    - configMapRef:
        name: my-config
