apiVersion: v1
kind: Pod
metadata:
  name: pod-drop-all
spec:
  containers:
  - name: drop-all
    image: alpine:3.19
    command: ["sh", "-c", "sleep 3600"]
    securityContext:
      capabilities:
        drop: ["ALL"]
        add: ["NET_ADMIN", "SYS_TIME"]  
